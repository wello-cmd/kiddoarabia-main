## 2024-05-14 - Improve AiBot Accessibility
**Learning:** Found that the main toggle for opening the `AiBot` is missing `aria-label` which causes issue for screen readers. Also, missing an aria label on the API button and Send button. Adding it for accessibility.
**Action:** Always add aria-labels on icon buttons like `AiBot` chat toggles, API button, and the send icon.
